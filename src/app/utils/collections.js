// Utils
import { envPrefix } from "utils/environment";


export const facilitiesCollection = `${envPrefix}_facilities`;

export const facilitiesSubmissionCollection = `${envPrefix}_facilities_submission`;